# Copy to .env and edit as needed.
# Docker Compose picks up .env automatically from the project root.

# MongoDB connection used by the backend container.
# Default points at the local mongo service defined in docker-compose.yml.
# To use your own MongoDB Atlas cluster instead, paste the full SRV URI here.
MONGO_URI=mongodb://mongo:27017/pharmacy
